defmodule Core.Factories.Period do
  @moduledoc false

  # alias Core.Schema.Period
  # import Core.FactoryPersister

  # defmacro __using__(_) do
  #   quote do
  #     def period_factory(params \\ []) do
  #       now = :os.system_time(:millisecond)

  #       fields =
  #         Keyword.merge(
  #           [
  #             start: now,
  #             end: now
  #           ],
  #           params
  #         )
  #         |> Enum.into(%{})

  #       struct(Period, fields)
  #     end
  #   end
  # end
end
